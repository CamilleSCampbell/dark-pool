services:
  - type: web
    name: hedgehog
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m backend.main
    envVars:
      - key: HEDGEHOG_MODE
        value: live
      - key: HEDGEHOG_PORT
        value: "8420"
