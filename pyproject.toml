[project]
name = "dark-pool"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "httpx>=0.27",
    "numpy>=1.26",
    "python-dotenv>=1.0",
]

[tool.vercel]
entrypoint = "app.py"
